name: Build

on:
  push:
    branches: [ "**" ]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '21'
        cache: gradle
    
    - name: Build with Gradle
      uses: gradle/gradle-build-action@v3
      with:
        gradle-version: 8.10.2
        arguments: build shadowJar
        
    - name: Upload jar artifact
      uses: actions/upload-artifact@v4
      with:
        name: TownyElections-geyser
        path: build/libs/TownyElections-geyser-*.jar
        if-no-files-found: error
    
    - name: Publish rolling nightly release
      if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        gh release delete nightly --yes --cleanup-tag || true
        gh release create nightly \
          --title "Nightly" \
          --notes "Automated nightly build from commit ${{ github.sha }} (updated on every push to main)." \
          --prerelease \
          --target main \
          build/libs/TownyElections-geyser-*.jar
